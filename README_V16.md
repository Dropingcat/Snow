# 📐 Полная математическая модель синтеза звука хруста снега (V16)

## 1. Общая структура сигнала

### 1.1. Дискретизация времени

$$t[n] = \frac{n}{f_s}, \quad n = 0, 1, 2, ..., N-1$$

где:
- $f_s = 44100$ Гц — частота дискретизации
- $N$ — общее количество сэмплов
- $T = N/f_s$ — общая длительность сигнала

### 1.2. Иерархия сигнала

$$\text{audio}[t] = \sum_{k=1}^{K} \text{step}_k[t - t_k] \cdot a_k$$

где:
- $K$ — количество шагов
- $t_k$ — время начала $k$-го шага
- $a_k \sim \mathcal{U}(0.85, 1.15)$ — случайная вариация амплитуды шага

---

## 2. Модель одного шага (Step Model)

### 2.1. Фазовая структура

$$\text{step}[t] = \begin{cases} 
\text{impact}[t] & 0 \leq t < T_1 \\
\text{compression}[t] & T_1 \leq t < T_2 \\
\text{shear}[t] & T_2 \leq t < T_3 \\
\text{release}[t] & T_3 \leq t < T_4 \\
\text{tail}[t] & T_4 \leq t < T_5
\end{cases}$$

### 2.2. Адаптивные длительности фаз

$$\begin{aligned}
T_1 &= 0.05 \cdot (1 + 0.1 \cdot (d_f - 1)) \\
T_2 &= T_1 + 0.15 \cdot (1 + 0.2 \cdot (p_f - 1)) \\
T_3 &= T_2 + 0.10 \cdot (1 + 0.5 \cdot (p_f - 1) \cdot d_f \cdot \text{drag}) \\
T_4 &= T_3 + 0.05 \cdot (1 + 0.3 \cdot (d_f - 1)) \\
T_5 &= T_4 + 0.15 \quad \text{(decay tail)}
\end{aligned}$$

где:
- $d_f = \text{clip}\left(\frac{\text{depth}}{0.15}, 0.5, 2.0\right)$ — фактор глубины
- $p_f = \text{clip}\left(\frac{\text{pressure}}{0.8}, 0.8, 1.5\right)$ — фактор давления
- $\text{drag} \in [0.5, 2.0]$ — параметр тягучести

### 2.3. Конвертация в сэмплы

$$N_i = \lfloor T_i \cdot f_s \rfloor, \quad i \in \{1,2,3,4,5\}$$

---

## 3. Спектрально-окрашенный шум (Spectral Shaped Noise)

### 3.1. Генерация белого шума с дитерингом

$$w[n] = \mathcal{N}(0, 1) + \mathcal{U}(-0.1, 0.1) \cdot \sigma_d$$

где $\sigma_d = 0.02$ — сила дитеринга

### 3.2. Прямое БПФ

$$W[k] = \sum_{n=0}^{N-1} w[n] \cdot e^{-j 2\pi k n / N}, \quad k = 0, 1, ..., N/2$$

### 3.3. Частотная сетка

$$f_k = \frac{k \cdot f_s}{N}, \quad k = 0, 1, ..., N/2$$

### 3.4. Целевая амплитудная огибающая (интерполяция эталона)

$$A_{\text{ref}}(f) = \text{interp}_{\text{linear}}\left(f, \{f_i^{\text{ref}}, A_i^{\text{ref}}\}_{i=1}^{M}\right)$$

где $\{f_i^{\text{ref}}, A_i^{\text{ref}}\}$ — точки спектральной огибающей эталонного звука

### 3.5. Roll-off фильтр (Гауссовый спад)

$$R(f) = \exp\left(-\left(\frac{f - f_{\text{start}}}{\sigma_r}\right)^2\right) \cdot \mathbb{I}(f > f_{\text{start}})$$

где:
- $f_{\text{start}} = 0.7 \cdot f_{\text{cutoff}}$
- $\sigma_r = 1500$ Гц
- $f_{\text{cutoff}} \approx 6500$ Гц

### 3.6. Сохранение высоких частот

$$A_{\text{HF}}(f) = \max\left(A_{\text{ref}}(f) \cdot R(f), \ 0.18 \cdot \max(A_{\text{ref}})\right) \cdot \mathbb{I}(f > f_{\text{cutoff}})$$

### 3.7. Центроидный буст

$$B(f) = 1 + 0.5 \cdot \exp\left(-\left(\frac{f - f_c}{0.5 \cdot f_c}\right)^2\right)$$

где $f_c \in [1000, 4000]$ Гц — целевой спектральный центроид

### 3.8. Случайные вариации амплитуды (анти-артефакт)

$$V(f) \sim \mathcal{U}(0.85, 1.15)$$

### 3.9. Итоговая спектральная огибающая

$$A_{\text{final}}(f) = A_{\text{ref}}(f) \cdot R(f) \cdot B(f) \cdot V(f)$$

### 3.10. Применение огибающей в частотной области

$$Y[k] = W[k] \cdot A_{\text{final}}(f_k)$$

### 3.11. Обратное БПФ

$$y[n] = \frac{1}{N} \sum_{k=0}^{N/2} Y[k] \cdot e^{j 2\pi k n / N}$$

---

## 4. Фаза Impact (Удар пятки)

### 4.1. Спектрально-окрашенный шум

$$x_{\text{impact}}[n] = \text{SpectralNoise}(N_1, f_c=3000\text{ Гц})$$

### 4.2. Экспоненциальное затухание

$$e_{\text{impact}}[n] = \exp\left(-4 \cdot \frac{n}{N_1}\right)$$

### 4.3. Оконная функция Ханна

$$h[n] = 0.5 \cdot \left(1 - \cos\left(\frac{2\pi n}{N_1-1}\right)\right)$$

### 4.4. Итоговый сигнал фазы

$$s_{\text{impact}}[n] = x_{\text{impact}}[n] \cdot e_{\text{impact}}[n] \cdot h[n]$$

---

## 5. Фаза Compression (Сжатие снега)

### 5.1. Спектрально-окрашенный шум

$$x_{\text{comp}}[n] = \text{SpectralNoise}(N_2-N_1, f_c=1500\text{ Гц})$$

### 5.2. Модель треска кристаллов

$$c[n] = 1 + \sum_{i=1}^{N_c} \alpha_i \cdot \mathbb{I}(p_i \leq n < p_i + d_i)$$

где:
- $N_c \sim \mathcal{U}(5, 15)$ — количество тресков
- $p_i \sim \mathcal{U}(0, N-100)$ — позиция $i$-го треска
- $d_i \sim \mathcal{U}(50, 300)$ — длительность треска (сэмплы)
- $\alpha_i \sim \mathcal{U}(1.2, 2.0)$ — усиление треска

### 5.3. Влияние температуры (crispness)

$$\gamma = \text{clip}\left(\frac{T + 15}{10}, 0.5, 2.0\right), \quad T \in [-25, 0]°C$$

### 5.4. Итоговый сигнал фазы

$$s_{\text{comp}}[n] = x_{\text{comp}}[n] \cdot c[n] \cdot \gamma$$

---

## 6. Фаза Shear (Скольжение/Перекатывание) — КРИТИЧЕСКАЯ ФАЗА

### 6.1. Базовый шум

$$x_{\text{shear}}[n] = \text{SpectralNoise}(N_3-N_2, f_c=1500\text{ Гц})$$

### 6.2. Низкочастотная модуляция (разрушение паттернов)

$$\begin{aligned}
m[n] &= \mathcal{N}(0, 1) \\
m_{\text{filt}}[n] &= \text{Butterworth}_{\text{low}}(m[n], f_{\text{cut}}=800\text{ Гц}) \\
m_{\text{norm}}[n] &= 0.3 \cdot \frac{m_{\text{filt}}[n]}{\sigma_{m_{\text{filt}}} + \epsilon} \\
M[n] &= 1 + 0.1 \cdot m_{\text{norm}}[n]
\end{aligned}$$

### 6.3. Тональный скрип с FM-модуляцией

#### 6.3.1. Частотная траектория

$$\begin{aligned}
f_{\text{start}} &= \text{clip}(0.5 \cdot f_{\text{centroid}}^{\text{max}}, 400, 800\text{ Гц}) \\
f_{\text{end}} &= \text{clip}(0.3 \cdot f_{\text{centroid}}^{\text{min}}, 80, 250\text{ Гц}) \\
f[n] &= f_{\text{start}} + (f_{\text{end}} - f_{\text{start}}) \cdot \frac{n}{N}
\end{aligned}$$

#### 6.3.2. Фаза с интегрированием частоты

$$\phi[n] = 2\pi \cdot \sum_{i=0}^{n} \frac{f[i]}{f_s}$$

#### 6.3.3. FM-модуляция (анти-гармоники)

$$\begin{aligned}
\beta &\sim \mathcal{U}(0.5, 2.0) \quad \text{(индекс модуляции)} \\
f_m &\sim \mathcal{U}(50, 200)\text{ Гц} \quad \text{(частота модулятора)} \\
\text{FM}[n] &= \sin(2\pi f_m t[n]) \cdot \beta
\end{aligned}$$

#### 6.3.4. Основная синусоида с гармониками

$$\begin{aligned}
q[n] &= A_s \cdot \sin(\phi[n] + \text{FM}[n]) \\
&+ 0.5 A_s \cdot \sin(2\phi[n] + 0.1 + 0.5\cdot\text{FM}[n]) \\
&+ 0.25 A_s \cdot \sin(3\phi[n] - 0.2 + 0.25\cdot\text{FM}[n])
\end{aligned}$$

где интенсивность скрипа:

$$A_s = \begin{cases} 
\text{clip}(0.15 \cdot p_f, 0.08, 0.18) & \text{если drag} > 1.5 \text{ или } p_f > 1.2 \\
\text{clip}(0.20 \cdot p_f, 0.10, 0.25) & \text{иначе}
\end{cases}$$

#### 6.3.5. Огибающая Ханна

$$h_{\text{squeak}}[n] = 0.5 \cdot \left(1 - \cos\left(\frac{2\pi n}{N-1}\right)\right)$$

#### 6.3.6. Итоговый скрип

$$s_{\text{squeak}}[n] = q[n] \cdot h_{\text{squeak}}[n]$$

### 6.4. Кривая смешивания (шум доминирует)

$$\mu[n] = 0.25 + 0.2 \cdot \sin\left(\pi \cdot \frac{n}{N}\right)$$

### 6.5. Смешивание шума и скрипа

$$s_{\text{shear}}[n] = (1 - \mu[n]) \cdot x_{\text{shear}}[n] \cdot M[n] + \mu[n] \cdot s_{\text{squeak}}[n]$$

---

## 7. Фаза Release (Отрыв стопы)

### 7.1. Белый шум с низкой амплитудой

$$x_{\text{release}}[n] \sim \mathcal{N}(0, 0.1)$$

### 7.2. Быстрое экспоненциальное затухание

$$e_{\text{release}}[n] = \exp\left(-6 \cdot \frac{n}{N_4}\right)$$

### 7.3. Итоговый сигнал фазы

$$s_{\text{release}}[n] = x_{\text{release}}[n] \cdot e_{\text{release}}[n]$$

---

## 8. Микрокристаллические импульсы

### 8.1. Генерация позиций без перекрытий

$$\begin{aligned}
&p_i \sim \mathcal{U}(0, N-20), \quad i = 1, ..., N_{\text{crystal}} \\
&N_{\text{crystal}} = \lfloor N / 10 \rfloor \\
&\text{Ограничение: } |p_i - p_j| \geq \frac{d_i + d_j}{2}, \quad \forall i \neq j
\end{aligned}$$

### 8.2. Форма импульса

$$\text{impulse}_i[n] = \begin{cases}
a_i \cdot \mathcal{N}(0, 1) \cdot h_{d_i}[n - p_i] & p_i \leq n < p_i + d_i \\
0 & \text{иначе}
\end{cases}$$

где:
- $a_i \sim \mathcal{U}(0.1, 0.4)$ — амплитуда
- $d_i \sim \mathcal{U}(5, 15)$ — длительность (сэмплы)
- $h_{d_i}$ — окно Ханна длиной $d_i$

### 8.3. Суммарный кристаллический сигнал

$$c_{\text{total}}[n] = \sum_{i=1}^{N_{\text{crystal}}} \text{impulse}_i[n]$$

### 8.4. Добавление к основному сигналу

$$s_{\text{with\_crystals}}[n] = s_{\text{phase}}[n] + 0.45 \cdot c_{\text{total}}[n]$$

---

## 9. Кроссфейд между фазами

### 9.1. Длины затухания

$$N_{\text{fade}} = \min\left(\lfloor 0.002 \cdot f_s \rfloor, \ \lfloor N_{\text{phase}}/4 \rfloor\right)$$

### 9.2. Функции плавного перехода

$$\begin{aligned}
f_{\text{in}}[n] &= \left(\frac{n}{N_{\text{fade}}-1}\right)^2 \\
f_{\text{out}}[n] &= \left(1 - \frac{n}{N_{\text{fade}}-1}\right)^2
\end{aligned}$$

### 9.3. Смешивание сигналов

$$s_{\text{crossfade}}[n] = \begin{cases}
s_{\text{prev}}[n] \cdot f_{\text{out}}[n] + s_{\text{curr}}[n] \cdot f_{\text{in}}[n] & 0 \leq n < N_{\text{fade}} \\
s_{\text{curr}}[n] & N_{\text{fade}} \leq n < N_{\text{curr}}
\end{cases}$$

---

## 10. Спектральное сглаживание (Анти-артефакт)

### 10.1. Прямое БПФ сигнала

$$X[k] = \text{FFT}(s[n])$$

### 10.2. Разделение на магнитуду и фазу

$$\begin{aligned}
|X[k]| &= \sqrt{\text{Re}(X[k])^2 + \text{Im}(X[k])^2} \\
\angle X[k] &= \arctan2(\text{Im}(X[k]), \text{Re}(X[k]))
\end{aligned}$$

### 10.3. Скользящее среднее для магнитуды

$$|X|_{\text{smooth}}[k] = \frac{1}{W} \sum_{i=-W/2}^{W/2} |X[k+i]|$$

где $W = \max(3, \lfloor K/200 \rfloor)$ — размер окна сглаживания

### 10.4. Смешивание оригинала и сглаженного

$$|X|_{\text{final}}[k] = 0.7 \cdot |X[k]| + 0.3 \cdot |X|_{\text{smooth}}[k]$$

### 10.5. Обратное БПФ

$$s_{\text{smooth}}[n] = \text{IFFT}(|X|_{\text{final}}[k] \cdot e^{j\angle X[k]})$$

---

## 11. Мягкий лимитер (Soft Limiter)

### 11.1. Порог срабатывания

$$\tau = 0.85$$

### 11.2. Функция сжатия

$$s_{\text{limited}}[n] = \begin{cases}
s[n] & |s[n]| \leq \tau \\
\text{sign}(s[n]) \cdot \left(\tau + \frac{|s[n]| - \tau}{1 + |s[n]| - \tau}\right) & |s[n]| > \tau
\end{cases}$$

---

## 12. Затухающий хвост (Decay Tail)

### 12.1. Длительность хвоста

$$N_{\text{tail}} = \lfloor 0.15 \cdot f_s \rfloor$$

### 12.2. Продолжение последнего значения с затуханием

$$s_{\text{tail}}[n] = \begin{cases}
s[n] & 0 \leq n < N \\
s[N-1] \cdot \exp\left(-5 \cdot \frac{n-N}{N_{\text{tail}}}\right) & N \leq n < N + N_{\text{tail}}
\end{cases}$$

---

## 13. Нормализация Peak/RMS

### 13.1. Вычисление текущего соотношения

$$\text{Peak/RMS}_{\text{current}} = \frac{\max(|s[n]|)}{\sqrt{\frac{1}{N}\sum_{n=0}^{N-1} s[n]^2} + \epsilon}$$

### 13.2. Целевое соотношение

$$\text{Peak/RMS}_{\text{target}} \approx 28.0$$

### 13.3. Расширение при низком соотношении

Если $\text{Peak/RMS}_{\text{current}} < 0.7 \cdot \text{Peak/RMS}_{\text{target}}$:

$$s_{\text{extended}}[n] = \begin{cases}
s[n] & 0 \leq n < N \\
s[N-1] \cdot \exp\left(-4 \cdot \frac{n-N}{N_{\text{extra}}}\right) & N \leq n < N + N_{\text{extra}}
\end{cases}$$

где $N_{\text{extra}} = \lfloor 0.3 \cdot N \rfloor$

### 13.4. Финальная нормализация к пику

$$s_{\text{final}}[n] = 0.9 \cdot \frac{s[n]}{\max(|s[n]|) + \epsilon}$$

---

## 14. Физический контекст

### 14.1. Температура → Crispness

$$\gamma(T) = \text{clip}\left(\frac{T + 15}{10}, 0.5, 2.0\right), \quad T \in [-25, 0]°C$$

### 14.2. Глубина снега → Уплотнение

$$d_{k+1} = d_k \cdot 0.997, \quad d_0 = 0.15\text{ м}$$

### 14.3. Давление ноги

$$p \in [0.2, 1.5] \quad \text{(безразмерный коэффициент)}$$

### 14.4. Тягучесть (Drag Factor)

$$\text{drag} \in [0.5, 2.0]$$

**Анти-артефакт ограничение:**
$$\text{drag}_{\text{effective}} = \begin{cases}
\text{clip}(\text{drag}, 0.5, 1.6) & \text{если drag} > 1.5 \text{ или depth} > 0.35 \\
\text{drag} & \text{иначе}
\end{cases}$$

---

## 15. Временная структура ходьбы

### 15.1. Базовый интервал между шагами

$$\Delta t_{\text{base}} = \frac{1}{f_{\text{step}}}, \quad f_{\text{step}} \in [0.8, 2.5]\text{ Гц}$$

### 15.2. Случайная вариация интервала

$$\Delta t_k = \Delta t_{\text{base}} \cdot \mathcal{U}(0.92, 1.08)$$

### 15.3. Позиции шагов

$$t_k = \sum_{i=1}^{k} \Delta t_i, \quad t_0 = 0$$

---

## 16. Итоговое уравнение полного сигнала

$$\boxed{\text{audio}[n] = 0.95 \cdot \frac{\displaystyle\sum_{k=1}^{K} s_{\text{step}}^{(k)}[n - n_k] \cdot a_k}{\displaystyle\max_n\left|\sum_{k=1}^{K} s_{\text{step}}^{(k)}[n - n_k] \cdot a_k\right| + \epsilon}}$$

где:
- $s_{\text{step}}^{(k)}$ — $k$-й шаг со всеми фазами и обработкой
- $n_k = \lfloor t_k \cdot f_s \rfloor$ — позиция начала $k$-го шага
- $a_k \sim \mathcal{U}(0.85, 1.15)$ — вариация амплитуды
- $\epsilon = 10^{-10}$ — численная стабильность

---

## 17. Сводка параметров модели

| Параметр | Обозначение | Диапазон | По умолчанию |
|----------|-------------|----------|--------------|
| Частота дискретизации | $f_s$ | — | 44100 Гц |
| Частота шагов | $f_{\text{step}}$ | [0.8, 2.5] Гц | 1.8 Гц |
| Температура | $T$ | [-25, 0] °C | -5 °C |
| Глубина снега | $d$ | [0.05, 0.5] м | 0.15 м |
| Давление ноги | $p$ | [0.2, 1.5] | 0.8 |
| Тягучесть | $\text{drag}$ | [0.5, 2.0] | 1.0 |
| Хруст кристаллов | $c_{\text{coef}}$ | [0.2, 0.8] | 0.45 |
| Целевой Peak/RMS | $R_{\text{target}}$ | — | 28.0 |
| Частота отсечки | $f_{\text{cutoff}}$ | — | 6500 Гц |
| Спектральный центроид | $f_c$ | [1000, 4000] Гц | 2000 Гц |

---

## 18. Блок-схема обработки сигнала

```
Белый шум + дитеринг
         ↓
    [БПФ]
         ↓
Спектральная огибающая (интерполяция эталона)
         ↓
Roll-off фильтр + HF сохранение + центроидный буст
         ↓
   [Обратное БПФ]
         ↓
┌────────────────────────────────────────────────────┐
│                 ФАЗОВАЯ СТРУКТУРА                   │
├──────────┬─────────────┬──────────┬────────────────┤
│ Impact   │ Compression │  Shear   │    Release     │
│ 0.05с    │   0.15с     │  0.10с   │     0.05с      │
│ + модуляция + треск    │ + скрип  │ + затухание    │
│          │ + crispness │ + FM     │                │
└──────────┴─────────────┴──────────────────────────┘
         ↓
    [Кроссфейд 2мс]
         ↓
Кристаллические импульсы (×0.45)
         ↓
Спектральное сглаживание (70/30)
         ↓
Мягкий лимитер (τ=0.85)
         ↓
Затухающий хвост (+0.15с)
         ↓
Нормализация Peak/RMS
         ↓
Финальная нормализация (пик=0.9)
         ↓
    ВЫХОДНОЙ СИГНАЛ
```

---

Эта математическая модель полностью описывает алгоритм синтеза без упрощений и может быть использована для реимплементации на любой платформе.

# Строгая математическая и физическая модель поведения кристаллов льда и снега в процессе хруста

## 1. Введение

Целью данной модели является описание микроскопических процессов, происходящих в снежном покрове при механическом воздействии (шаге), и их связи с генерируемым акустическим сигналом. Модель строится на основе физики гранулированных сред, механики разрушения и акустики. Она включает:

- Динамику напряжённо-деформированного состояния снега.
- Критерии разрушения ледяных связей и кристаллов.
- Процессы трения и скольжения.
- Акустическую эмиссию от каждого микрособытия.
- Суперпозицию сигналов для получения итогового звука.

## 2. Микроструктурное описание снега

Снег рассматривается как **дискретная среда**, состоящая из ледяных частиц (кристаллов) и пор, заполненных воздухом. Частицы контактируют друг с другом, образуя перешейки (шейки спекания). Введём следующие переменные:

### 2.1. Индивидуальные характеристики частиц

Для каждой частицы \( i \) (сферическое приближение или эллипсоид) задаются:

- **Радиус** \( R_i \) (или эквивалентный диаметр \( D_i \))
- **Координаты центра масс** \( \mathbf{r}_i \)
- **Скорость** \( \mathbf{v}_i \), угловая скорость \( \boldsymbol{\omega}_i \)
- **Ориентация** (если форма несферическая)
- **Масса** \( m_i = \rho_i V_i \), где \( \rho_i \approx 917 \, \text{кг/м}^3 \) (плотность льда), \( V_i \) — объём
- **Момент инерции** \( I_i \)

### 2.2. Контактные взаимодействия

Контакт между частицами \( i \) и \( j \) характеризуется:

- **Вектором контакта**: \( \mathbf{n}_{ij} = (\mathbf{r}_j - \mathbf{r}_i)/|\mathbf{r}_j - \mathbf{r}_i| \) (нормаль)
- **Перекрытием** (или зазором) \( \delta_{ij} \). При наличии перешейка перекрытие может быть отрицательным (связь работает на растяжение).
- **Площадью контакта** \( A_{ij} \) (зависит от размера перешейка).
- **Типом связи**: упругая, пластическая, разрушенная.

### 2.3. Свойства перешейков

Перешейки представляют собой ледяные мостики, образовавшиеся в результате спекания. Их механические свойства зависят от температуры и времени. Для упрощения будем считать, что каждый контакт обладает:

- **Нормальной жёсткостью** \( k_n \)
- **Касательной жёсткостью** \( k_t \)
- **Прочностью на разрыв** \( \sigma_{\text{ten}} \)
- **Прочностью на сдвиг** \( \sigma_{\text{shear}} \)
- **Коэффициентом вязкого демпфирования** \( \eta \)

Эти параметры могут быть функциями температуры \( T \) и размера перешейка.

### 2.4. Пористость и плотность

Макроскопическая плотность снега \( \rho_{\text{snow}} \) связана с плотностью льда и пористостью \( \phi \):
\[
\rho_{\text{snow}} = \rho_i (1 - \phi).
\]
Пористость определяет акустический импеданс и скорость звука в снеге.

## 3. Динамика системы

### 3.1. Уравнения движения частиц

Для каждой частицы \( i \) (в приближении дискретных элементов) уравнения Ньютона–Эйлера:

\[
m_i \frac{d\mathbf{v}_i}{dt} = \sum_{j \in \text{contacts}} \mathbf{F}_{ij} + \mathbf{F}_{\text{ext}} + \mathbf{F}_{\text{damp}},
\]
\[
I_i \frac{d\boldsymbol{\omega}_i}{dt} = \sum_{j \in \text{contacts}} \mathbf{M}_{ij},
\]

где:
- \( \mathbf{F}_{ij} \) — сила взаимодействия с частицей \( j \),
- \( \mathbf{F}_{\text{ext}} \) — внешние силы (гравитация, давление ноги),
- \( \mathbf{F}_{\text{damp}} \) — диссипативные силы (вязкость среды),
- \( \mathbf{M}_{ij} \) — момент силы.

### 3.2. Модель контактной силы

Используем упруго-вязкую модель с разрушением. Сила в контакте раскладывается на нормальную и касательную компоненты:

\[
\mathbf{F}_{ij} = F_n \mathbf{n}_{ij} + \mathbf{F}_t,
\]
\[
\mathbf{F}_t = F_t \mathbf{t}_{ij},
\]
где \( \mathbf{t}_{ij} \) — единичный вектор в плоскости контакта.

#### 3.2.1. Нормальная сила

Для упругого контакта Герца (сфера-сфера) или для плоского контакта перешейка можно использовать линейную модель:

\[
F_n = k_n \delta_n - \eta_n v_n,
\]
где \( \delta_n \) — нормальное перекрытие (положительное при сжатии, отрицательное при растяжении), \( v_n \) — относительная нормальная скорость. Жёсткость \( k_n \) зависит от размера контакта и модуля Юнга льда \( E \). При наличии перешейка:
\[
k_n = \frac{E A_{ij}}{h},
\]
где \( A_{ij} \) — площадь сечения перешейка, \( h \) — его длина (можно считать \( h \approx 0 \)).

#### 3.2.2. Касательная сила

Касательная сила развивается по закону упругого нагружения с трением. Используем модель с кулоновским трением:

\[
\mathbf{F}_t = k_t \boldsymbol{\delta}_t - \eta_t \mathbf{v}_t,
\]
при условии, что \( |\mathbf{F}_t| \leq \mu F_n \) (для скольжения без разрушения связи). Здесь \( \boldsymbol{\delta}_t \) — относительное касательное смещение, \( \mathbf{v}_t \) — касательная скорость. Если превышен предел, происходит проскальзывание.

Для разрушения перешейка на сдвиг вводится предельное касательное напряжение:
\[
F_t^{\max} = \sigma_{\text{shear}} A_{ij}.
\]

### 3.3. Критерии разрушения

Разрушение контакта происходит, если:
- Нормальное растяжение превышает прочность: \( -F_n > \sigma_{\text{ten}} A_{ij} \).
- Касательная сила превышает прочность на сдвиг: \( |F_t| > \sigma_{\text{shear}} A_{ij} \).
- Комбинированный критерий (например, Мизеса):
\[
\left( \frac{\max(0, -F_n)}{\sigma_{\text{ten}} A_{ij}} \right)^2 + \left( \frac{|F_t|}{\sigma_{\text{shear}} A_{ij}} \right)^2 \geq 1.
\]

При разрушении контакт исчезает, высвобождается упругая энергия, которая частично переходит в акустическое излучение.

### 3.4. Акустическая эмиссия от разрушения

Каждое разрушение перешейка или кристалла генерирует акустический импульс. Будем моделировать его как излучение от точечного источника (дипольного или монопольного типа) с силой, пропорциональной изменению силы во времени.

Для простоты примем, что импульс силы \( \mathbf{f}(t) \), действовавший в контакте до разрушения, резко спадает до нуля за время \( \tau \) (время релаксации). Тогда акустическое давление в точке наблюдения \( \mathbf{r} \) от источника в точке \( \mathbf{r}_s \) для дипольного излучения (пара сил) можно записать как:

\[
p(\mathbf{r}, t) = \frac{1}{4\pi c} \frac{\partial}{\partial t} \left[ \frac{\mathbf{n} \cdot \mathbf{f}(t - |\mathbf{r} - \mathbf{r}_s|/c)}{|\mathbf{r} - \mathbf{r}_s|} \right],
\]
где \( \mathbf{n} = (\mathbf{r} - \mathbf{r}_s)/|\mathbf{r} - \mathbf{r}_s| \), \( c \) — скорость звука в снеге. Для упрощения в численных схемах часто используют вейвлет-форму импульса, например, производную гауссиана.

Характерная частота излучения обратно пропорциональна времени разрушения \( \tau \). Время \( \tau \) можно оценить как \( \tau \approx L / v_{\text{crack}} \), где \( L \) — размер области разрушения, \( v_{\text{crack}} \) — скорость распространения трещины (доли скорости звука). Для льда \( v_{\text{crack}} \sim 1000 \, \text{м/с} \), \( L \sim 1 \, \text{мм} \) даёт \( \tau \sim 1 \, \text{мкс} \), что соответствует частотам порядка 1 МГц — ультразвук. Однако в снеге высокие частоты сильно затухают, и мы слышим лишь низкочастотную часть спектра. Поэтому в модели можно использовать эмпирическую форму импульса с пониженной частотой, но физически обоснованную.

### 3.5. Трение и скрип

При скольжении контактирующих поверхностей возникает сила трения, которая может приводить к автоколебаниям (stick-slip). Модель трения должна учитывать зависимость коэффициента трения от скорости (эффект Штрибека) и состояния поверхности. Для льда трение сильно зависит от температуры и наличия водяной плёнки.

Процесс скольжения генерирует акустический сигнал как результат флуктуаций силы трения. Можно моделировать его как случайный процесс с корреляционной функцией, определяемой шероховатостью и скоростью.

### 3.6. Выдавливание воздуха

При сжатии пористого снега воздух вытесняется из пор, создавая шум. Этот процесс можно описать уравнениями фильтрации (закон Дарси) и акустикой потока. Однако для хруста основной вклад дают разрушения и трение, поэтому выдавливание воздуха можно считать второстепенным.

## 4. Макроскопические поля и усреднение

Для связи с макроскопической деформацией и напряжением введём осреднённые величины в представительном объёме \( V \):

- **Тензор напряжений** \( \boldsymbol{\sigma} = \frac{1}{V} \sum_{\text{contacts}} \mathbf{F}_{ij} \otimes \mathbf{l}_{ij} \), где \( \mathbf{l}_{ij} \) — вектор от центра одной частицы к центру другой.
- **Тензор деформаций** \( \boldsymbol{\varepsilon} \) из перемещений частиц.
- **Плотность энергии** \( U = \frac{1}{2V} \sum_{\text{contacts}} \left( \frac{F_n^2}{k_n} + \frac{|\mathbf{F}_t|^2}{k_t} \right) \).

Скорость диссипации энергии идёт на акустическую эмиссию и тепло.

## 5. Стохастическое описание

Из-за большого числа частиц и случайности их расположения можно перейти к вероятностному описанию. Введём функции распределения размеров кристаллов, прочности контактов и т.д. Тогда скорость разрушений в единице объёма \( \dot{n}_{\text{break}} \) определяется интегралом по распределению напряжений:

\[
\dot{n}_{\text{break}} = \int_0^\infty \int_0^\infty P_{\text{break}}(\sigma, T, R) \, f(\sigma, R) \, d\sigma \, dR,
\]
где \( f(\sigma, R) \) — плотность вероятности локальных напряжений и размеров, \( P_{\text{break}} \) — вероятность разрушения в единицу времени (например, из теории Аррениуса для термически активированного разрушения или из критерия превышения предела прочности).

## 6. Акустический сигнал на макроуровне

Итоговое звуковое давление в точке наблюдения (например, на расстоянии 1 м от поверхности) получается суперпозицией вкладов от всех микрособытий:

\[
p(t) = \sum_{\text{events } k} p_k(t - t_k),
\]
где \( p_k(t) \) — форма импульса от события \( k \), зависящая от его типа, энергии и местоположения. Для дальнего поля можно использовать приближение свёртки с функцией Грина среды, учитывающей затухание и дисперсию.

В простейшем случае, когда все события независимы и происходят случайно во времени, спектр мощности сигнала будет пропорционален спектру одиночного импульса, умноженному на среднюю частоту событий. Это объясняет, почему в феноменологической модели используется окрашенный шум.

## 7. Учёт температуры и внешних условий

Температура \( T \) влияет на:

- **Модуль упругости льда** \( E(T) \) (слабо, но при приближении к 0°C уменьшается).
- **Прочность перешейков** \( \sigma_{\text{ten}}(T) \), \( \sigma_{\text{shear}}(T) \) (сильно: при низких температурах лёд хрупкий, прочность высока; при высоких появляется жидкая плёнка, прочность падает).
- **Коэффициент трения** \( \mu(T) \) (обычно уменьшается с ростом температуры из-за смазки).
- **Скорость звука** в снеге \( c(T, \rho) \).

Внешнее давление от ноги задаётся как граничное условие: распределённая нагрузка на верхней поверхности снега.

## 8. Численная реализация

Полная модель требует решения системы дифференциальных уравнений для тысяч частиц методом дискретных элементов (DEM). Однако для практического синтеза звука можно использовать упрощения:

- Использовать DEM только для небольшого объёма, чтобы получить статистику событий, а затем генерировать звук как случайный процесс с заданными спектральными характеристиками, зависящими от макроскопических параметров.
- Применить континуальную модель с источниками акустической эмиссии, где скорость событий и распределение энергий определяются из решения задачи теории упругости с разрушением.

Таким образом, строгая модель включает:

1. **Микромеханику**: DEM с разрушаемыми связями.
2. **Акустику**: расчёт поля давлений от каждого события.
3. **Статистику**: осреднение для перехода к макропараметрам.

## 9. Математическая формулировка

Ниже приведены ключевые уравнения в компактной форме.

### 9.1. Динамика частиц

\[
\dot{\mathbf{r}}_i = \mathbf{v}_i,
\]
\[
m_i \dot{\mathbf{v}}_i = \sum_j \left( F_{n,ij} \mathbf{n}_{ij} + \mathbf{F}_{t,ij} \right) + m_i \mathbf{g} + \mathbf{F}_{\text{ext},i},
\]
\[
I_i \dot{\boldsymbol{\omega}}_i = \sum_j \mathbf{r}_{ij} \times \mathbf{F}_{t,ij},
\]
где \( \mathbf{r}_{ij} = \mathbf{r}_j - \mathbf{r}_i \).

### 9.2. Контактные силы

\[
F_{n,ij} = k_{n,ij} \delta_{n,ij} - \eta_n \dot{\delta}_{n,ij},
\]
\[
\mathbf{F}_{t,ij} = \min\left( k_{t,ij} \boldsymbol{\delta}_{t,ij} - \eta_t \dot{\boldsymbol{\delta}}_{t,ij}, \ \mu |F_{n,ij}| \right) \cdot \text{sign}( \boldsymbol{\delta}_{t,ij} ),
\]
с обновлением касательного смещения \( \boldsymbol{\delta}_{t,ij} \) с учётом вращения.

### 9.3. Критерий разрушения контакта

Если контакт существует (связь не разрушена), проверяем условие:
\[
\left( \frac{\max(0, -F_{n,ij})}{F_{\text{ten},ij}} \right)^2 + \left( \frac{|\mathbf{F}_{t,ij}|}{F_{\text{shear},ij}} \right)^2 \geq 1,
\]
где предельные силы \( F_{\text{ten},ij} = \sigma_{\text{ten}} A_{ij} \), \( F_{\text{shear},ij} = \sigma_{\text{shear}} A_{ij} \). При выполнении контакт удаляется, и генерируется акустическое событие.

### 9.4. Акустическое излучение от разрушения

Для каждого разрушенного в момент \( t_0 \) контакта с силой \( \mathbf{F}(t) \), которая обнуляется за время \( \tau \), излучаемое давление:
\[
p(\mathbf{r}, t) = \frac{1}{4\pi c} \frac{\partial}{\partial t} \left[ \frac{ \mathbf{n} \cdot \mathbf{F}(t - t_0 - |\mathbf{r} - \mathbf{r}_c|/c) }{ |\mathbf{r} - \mathbf{r}_c| } \right],
\]
где \( \mathbf{F}(t) \) можно аппроксимировать как \( \mathbf{F}_0 \cdot e^{-t/\tau} H(t) \) (экспоненциальный спад), а производная даёт двухполярный импульс.

### 9.5. Затухание звука в снеге

Учитываем частотно-зависимое затухание: коэффициент поглощения \( \alpha(f) \approx \alpha_0 f^2 \) (для пористых сред). Тогда спектр сигнала умножается на \( \exp(-\alpha(f) R) \).

### 9.6. Связь с макроскопическими параметрами

Для перехода к феноменологической модели из предыдущей версии можно выразить:

- **Количество тресков в единицу времени** \( \dot{N}_c \) пропорционально объёму разрушений и скорости деформации.
- **Распределение амплитуд тресков** \( P(A) \) следует степенному закону (как в сейсмологии).
- **Спектральная огибающая** определяется формой импульса и затуханием.

## 10. Заключение

Предложенная строгая модель связывает微观структурные параметры снега (размер кристаллов, прочность контактов, пористость) с макроскопическим акустическим сигналом. Она включает уравнения движения дискретных элементов, контактные взаимодействия с разрушением, акустическую эмиссию и распространение волн. Для численной реализации требуется DEM-моделирование, но возможно упрощение до стохастической модели, параметры которой определяются из физических соображений. Эта модель позволяет не только синтезировать реалистичный звук хруста снега, но и решать обратную задачу: по записи звука оценивать состояние снежного покрова.
Благодарю вас за этот блестящий, глубокий и конструктивный анализ! Вы не только выделили сильные стороны модели, но и указали на ключевые пробелы, которые действительно необходимы для перехода от феноменологического синтеза к строгой физической модели. Ваши замечания и предложения — это именно те «кирпичики», которых не хватало для полноты картины.

Ниже я по пунктам отвечу на каждый раздел вашего анализа, а затем предложу план интеграции ваших идей в итоговую версию модели.

---

## 1. Проблема масштаба частот (ультразвук → слышимый диапазон)

**Вы абсолютно правы.** Прямое использование времени разрушения кристалла даёт мегагерцовый диапазон, не слышимый человеком. Ваше предложение ввести низкочастотную модуляцию через частотно-зависимое затухание (пункт 4) фактически решает эту проблему. В модели V16 мы уже использовали эмпирический roll-off фильтр, но физическое обоснование теперь ясно: высокие частоты экспоненциально затухают в снежной толще. Мы добавим в модель явную зависимость коэффициента затухания от частоты и глубины:

$$\alpha(f, z) = \alpha_0 \left(\frac{f}{f_0}\right)^2 \cdot \frac{z}{z_0},$$

где $z$ — глубина источника, $z_0$ — масштаб (например, 1 м). Тогда спектр на поверхности будет:

$$S_{\text{surf}}(f) = S_{\text{src}}(f) \cdot e^{-\alpha(f, z)}.$$

Для упрощения в синтезе можно оставить гауссов roll-off, но теперь его параметры будут связаны с физическими величинами (плотность, глубина).

---

## 2. Модель спекания (sintering)

Это важнейшее дополнение! Эволюция перешейков действительно определяет, как меняется звук со временем (свежий снег vs старый). Ваше уравнение Kuczynski мы включим в модель как динамическую переменную для каждого контакта. Однако для синтеза звука в реальном времени мы можем использовать упрощённый подход: ввести параметр «возраст снега» $t_{\text{age}}$ и модифицировать прочность контактов:

$$\sigma_{\text{ten}}(t_{\text{age}}) = \sigma_{\text{ten}}^0 \cdot \left(1 - e^{-t_{\text{age}}/\tau_{\text{sinter}}}\right),$$

где $\tau_{\text{sinter}}$ — характерное время спекания (часы–дни, зависит от температуры). Это позволит V16 генерировать звук «старого» снега с более низкими частотами и меньшим количеством тресков.

---

## 3. Модель трения с фазовым переходом

Предложенная вами формула для коэффициента трения с учётом водяной плёнки — отличное уточнение. В V16 интенсивность скрипа $A_s$ уже зависит от давления и «тягучести», но теперь мы можем привязать «тягучесть» непосредственно к температуре:

$$\text{drag} = \mu(T, v) / \mu_0,$$

где $\mu(T, v)$ — ваше выражение. Это автоматически свяжет скрип с температурой. Для упрощения можно оставить параметр `drag` как входной, но добавить пресеты для разных температур.

---

## 4. Акустическое затухание (численные значения)

Спасибо за численные оценки! Мы включим $\alpha_0 = 0.1\,\text{дБ/(м·кГц}^2)$ в модель как константу, а для разных плотностей снега можно использовать эмпирическую зависимость (например, из работ Johnson, 1987). Это позволит точнее настроить roll-off фильтр.

---

## 5. Связь микро- и макроуровня

Ваша таблица соответствия — это именно тот мост, который нужен для перехода от DEM к синтезу. Мы возьмём её за основу и для каждого параметра синтеза V16 пропишем формулу, связывающую его с физическими величинами. Например:

- `crystal_coefficient` $= k_1 \cdot \dot{N} \cdot V_{\text{foot}}$, где $\dot{N}$ — скорость разрушений из DEM, $V_{\text{foot}}$ — объём под подошвой.
- `f_centroid` $= k_3 / \langle R \rangle$, где $\langle R \rangle$ — средний размер кристалла.

Коэффициенты $k_i$ будем калибровать по экспериментальным записям.

---

## 6. Missing physics

### 6.1 Фазовый переход под давлением
Хотя понижение температуры плавления мало при типичных давлениях ноги, мы всё же добавим этот эффект для полноты. В модели V16 это может проявиться только при экстремальных давлениях (например, прыжок), поэтому оставим как опциональный флаг.

### 6.2 Реверберация в снежном слое
Отличная идея! Добавим в V16 простой ревербератор с задержкой $\tau = 2d/c$ и коэффициентом отражения $r \approx 0.3$. Это создаст эффект «глубины» снега. В текущей версии уже есть «хвост» (tail), но это просто затухание, а не множественные отражения. Мы реализуем свёртку с простым импульсным откликом вида $h(t) = \sum_{m=0}^{M} r^m \delta(t - m\tau)$.

### 6.3 Нелинейное затухание
При больших амплитудах затухание действительно может расти. В V16 мы уже ввели анти-артефакт ограничение `drag_effective` при высоком давлении. Теперь мы можем обосновать это физически: $\alpha_{\text{eff}} = \alpha_0(1 + \beta A^2)$. В синтезе это можно реализовать как дополнительный low-pass фильтр, который становится сильнее при больших амплитудах.
